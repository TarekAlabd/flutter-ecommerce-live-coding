import 'package:flutter/material.dart';

class BagsPage extends StatelessWidget {
  const BagsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Bags');
  }
}
